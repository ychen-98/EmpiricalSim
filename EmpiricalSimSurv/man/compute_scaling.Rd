% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scaling.R
\name{compute_scaling}
\alias{compute_scaling}
\title{Compute Scaling Parameters (tau, delta) for Survival Simulation}
\usage{
compute_scaling(
  hist_data,
  target_val_low,
  target_val_high,
  q_scale_range = c(0.1, 0.9),
  obs_time = NULL,
  status = NULL,
  use_km = FALSE
)
}
\arguments{
\item{hist_data}{Historical time data (numeric vector, can be imputed).}

\item{target_val_low, target_val_high}{Target time values at the
lower/upper scaling quantile levels.}

\item{q_scale_range}{Two quantile levels, e.g. \code{c(0.1, 0.9)}.}

\item{obs_time, status}{Optional observed times and event indicators
for KM-based quantile extraction.}

\item{use_km}{If TRUE and obs_time/status given, use KM percentiles
for the historical arm instead of empirical quantiles.}
}
\value{
List with \code{tau}, \code{delta}, \code{hist_val_low},
  \code{hist_val_high}.
}
\description{
Derives a log-linear mapping  \eqn{Y = exp(log(X) * tau + delta)} that
aligns two quantiles of the historical distribution with two target values.
}
