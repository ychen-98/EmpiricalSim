% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_alpha_beta.R
\name{find_alpha_beta}
\alias{find_alpha_beta}
\title{Find Optimal Alpha/Beta via Two-Stage Adaptive Grid Search (Vectorized)}
\usage{
find_alpha_beta(
  a1 = 0.2,
  a2 = 2,
  a_step = 0.01,
  b1 = 0,
  b2 = 0.15,
  b_step = 0.001,
  relax_index = 3,
  search_index_alpha = 5,
  tau,
  delta,
  datref,
  tarmean = NULL,
  tarsd = NULL,
  type,
  target_med = NULL,
  target_low = NULL,
  target_high = NULL,
  q_low = 0.3,
  q_med = 0.5,
  q_high = 0.7
)
}
\arguments{
\item{a1, a2, a_step}{Alpha search range and step size (Stage 1).}

\item{b1, b2, b_step}{Beta search range and step size (Stage 1).}

\item{relax_index}{Relaxation factor for the two-step selection filter.}

\item{search_index_alpha}{Width of Stage 2 refinement window.}

\item{tau, delta}{Linear (mean/SD types) or log-scale (survival) shift.}

\item{datref}{Reference data — data.frame (first column used) or vector.}

\item{tarmean, tarsd}{Target mean and SD (required for non-survival).}

\item{type}{One of "continuous", "binary", "ordinal", "survival".}

\item{target_med, target_low, target_high}{Target time values (survival).}

\item{q_low, q_med, q_high}{Quantile levels (default 0.30, 0.50, 0.70).}
}
\value{
A list with Stage 1/2 grids, best parameters, and diagnostics.
}
\description{
Maps a historical reference distribution to match target summary statistics
using a power-distortion transformation  u^alpha * (1-u)^beta.
}
\section{Data types}{

  continuous / binary / ordinal  -> match target mean & SD
  survival                       -> match target percentiles
}

\section{Naming convention (survival)}{

  q_low, q_med, q_high           = quantile LEVELS (e.g. 0.30, 0.50, 0.70)
  target_low, target_med, target_high = target VALUES at those levels
}

