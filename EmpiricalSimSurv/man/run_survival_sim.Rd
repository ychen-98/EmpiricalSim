% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines.R
\name{run_survival_sim}
\alias{run_survival_sim}
\title{Run Full Empirical Simulation Pipeline (Survival Data)}
\usage{
run_survival_sim(
  target_percentiles = NULL,
  target_time = NULL,
  target_status = NULL,
  hist_time,
  hist_status,
  N_sim = 5000,
  n_quantiles = 5,
  margin_frac = 0.1,
  min_neff = 10,
  q_probs_manual = NULL,
  use_km_scaling = FALSE,
  a1 = 0.2,
  a2 = 2,
  a_step = 0.01,
  b1 = 0,
  b2 = 0.2,
  b_step = 0.001,
  relax_index = 5,
  search_index_alpha = 7,
  rho_matrix = NULL,
  verbose = TRUE
)
}
\arguments{
\item{target_percentiles}{A data.frame with columns \code{q_level}
and \code{time_val}.  At least 3 rows required.  When supplied,
\code{target_time}/\code{target_status} are ignored.}

\item{target_time, target_status}{Raw target arm data (optional backup).}

\item{hist_time, hist_status}{Historical/reference arm data.}

\item{N_sim}{Number of simulated observations.}

\item{n_quantiles}{Grid size for auto quantile levels (data path).}

\item{margin_frac}{Margin trimming fraction (data path).}

\item{min_neff}{N_eff threshold (data path).}

\item{q_probs_manual}{If supplied with data path, overrides auto levels.}

\item{use_km_scaling}{Use KM-based percentiles for scaling.}

\item{a1, a2, a_step, b1, b2, b_step}{Grid search parameters.}

\item{relax_index, search_index_alpha}{Two-stage tuning.}

\item{rho_matrix}{Optional external correlation matrix.}

\item{verbose}{Print diagnostics.}
}
\value{
List: sim_data, scales, shape_params, target_km, hist_km,
  q_probs_used, q_probs_mode, min_neff_used.
}
\description{
End-to-end pipeline for survival-only simulations.  Accepts EITHER:
  (a) \code{target_percentiles} — a data.frame(q_level, time_val) of
      pre-specified percentile targets (primary interface), OR
  (b) \code{target_time} / \code{target_status} — raw patient-level data
      from which KM percentiles are extracted (backup / convenience).
}
