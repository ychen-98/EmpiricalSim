% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_missing.R
\name{impute_missing}
\alias{impute_missing}
\title{Impute Missing Values in a Reference / Target Data Frame (Non-Survival)}
\usage{
impute_missing(
  dat,
  types,
  continuous_method = "mean",
  skip_cols = NULL,
  max_missing_frac = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{dat}{A data frame.}

\item{types}{Character vector of column types, length = \code{ncol(dat)}.
One of \code{"continuous"}, \code{"binary"}, \code{"ordinal"},
\code{"survival"}.}

\item{continuous_method}{\code{"mean"} (default) or \code{"median"} for
continuous columns.}

\item{skip_cols}{Optional character vector of column names to leave
untouched (e.g. survival time/status columns).}

\item{max_missing_frac}{Numeric in \code{(0, 1]}. If the missing fraction
in any imputable column exceeds this, a warning is emitted (the column is
still imputed). Default \code{0.5}.}

\item{verbose}{If TRUE, print a per-column summary of what was imputed.}
}
\value{
A list with elements \code{data} (the filled data frame),
\code{report} (a per-column data.frame), and \code{any_remaining}
(logical, TRUE if any \code{NA}/\code{NaN} remain).
}
\description{
Fills \code{NA}/\code{NaN} entries in the non-survival columns of a data
frame using simple, widely used, type-aware rules, so that downstream
functions (\code{\link{find_alpha_beta}}, \code{\link{generate_data}},
\code{\link{run_simulation}}) can run. Survival columns are not imputed
here; censored survival times are handled by \code{\link{impute_censored}}.
}
\section{Imputation rules}{

\describe{
  \item{continuous}{mean (default) or median.}
  \item{binary}{mode (most frequent level; ties to larger code).}
  \item{ordinal}{mode (rounded to an observed category).}
}
}

\examples{
df <- data.frame(age = c(50, NA, 60, NaN, 45),
                 sex = c(1, 0, NA, 1, 0),
                 ecog = c(0, 2, 2, NA, 1))
out <- impute_missing(df, types = c("continuous", "binary", "ordinal"))
out$data
out$report
}
