% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{impute_censored}
\alias{impute_censored}
\title{Impute Censored Observations Using KM Tail with Parametric Fallback}
\usage{
impute_censored(
  obs_time,
  status,
  seed = 123,
  min_neff = 10,
  verbose = TRUE,
  candidate_dists = c("weibull", "lognormal", "llogis", "gengamma", "gompertz"),
  tail_weight_fraction = 0.5,
  tail_weight_multiplier = 3,
  blend_window = 0,
  M = 1,
  s_param_floor = 1e-06
)
}
\arguments{
\item{obs_time}{Numeric vector of observed times.}

\item{status}{Event indicator (1 = event, 0 = censored).}

\item{seed}{Random seed for reproducibility.}

\item{min_neff}{Minimum Peto's N_eff required to trust the KM estimate.}

\item{verbose}{If TRUE, print diagnostics.}

\item{candidate_dists}{Character vector of flexsurv distribution names.}

\item{tail_weight_fraction}{Fraction of trunc_time for tail weighting.}

\item{tail_weight_multiplier}{Extra weight for tail observations.}

\item{blend_window}{Width of blending window around trunc_time.}

\item{M}{Number of multiply-imputed datasets to return.}

\item{s_param_floor}{Minimum S_param(c) for inverse-CDF sampling.}
}
\value{
If M == 1, a list with imputed data and diagnostics.
  If M > 1, a list with \code{imputations} and \code{shared} info.
}
\description{
Uses a KM curve for early censoring and falls back to a parametric model
(selected by tail-weighted AIC) for late censoring. The truncation point
is determined by Peto's effective sample size (N_eff).
}
